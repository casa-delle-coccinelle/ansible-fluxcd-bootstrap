---
galaxy_info:
  role_name: fluxcd-bootstrap

dependencies:
  - role: geerlingguy.pip
    pip_install_packages:
      - name: packaging
      - name: setuptools
      - name: requests
      - name: docker
      - name: kubernetes
      - name: jsonpatch
      - name: PyYAML
